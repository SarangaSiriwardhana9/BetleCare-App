import 'package:flutter/material.dart';

class WeatherScreen3 extends StatelessWidget {
  const WeatherScreen3({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Monthly Weather Forecast Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}