import 'package:flutter/material.dart';

class WeatherScreen4 extends StatelessWidget {
  const WeatherScreen4({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Weather Summary Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}